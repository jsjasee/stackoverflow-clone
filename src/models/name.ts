// we are consolidating all the names into a bunch of constants so that we can rename it in the future if we wish to.
export const db = "main-stackflow"; // this is the name of the db
export const questionCollection = "questions";
export const answerCollection = "answers";
export const commentCollection = "comments";
export const voteCollection = "votes";
export const questionAttachmentBucket = "question-attachments"; // this is for like when we need to attach images to our questions.
